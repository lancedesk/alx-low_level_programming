#include "main.h"

/**
 * _strcpy - copies string
 * @dest: string destination
 * @src: source of string
 * Return: pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	char *destination = strcpy(dest, src);

	return (destination);
}
