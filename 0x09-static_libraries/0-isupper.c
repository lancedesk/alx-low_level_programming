#include "main.h"

/**
 * _isupper - tests if uppercase
 * @c: integer input
 * Return: returns 1 if uppercase
 * and 0 if lowercase
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
