#include "main.h"

/**
 * _islower - checks if variable islower
 * @c: character input
 * Return: 1 for islower and 0 for !
 */

int _islower(int c)
{
if (islower(c))
{
	return (1);
}
else
{
	return (0);
}
}
